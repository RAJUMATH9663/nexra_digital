# Base URL of the Django REST API backend.
# Leave unset to run the site fully with static/mock data (no backend required).
# Example once Django is deployed:
# NEXT_PUBLIC_API_BASE_URL=https://api.nexradigital.com/api
NEXT_PUBLIC_API_BASE_URL=
